apply plugin: 'com.android.library'

android {
    namespace = "kankan.wheel"
    compileSdkVersion apiVersion

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}
